import {StrictMode} from 'react';
import {createRoot} from 'react-dom/client';
import App from './App.tsx';
import { ErrorBoundary } from './components/ErrorBoundary.tsx';
import { setupClientAISystem } from './lib/clientAiLogger.ts';
import './index.css';

// Initialize the global client-side AI error interception & logger proxying
setupClientAISystem();

createRoot(document.getElementById('root')!).render(
  <StrictMode>
    <ErrorBoundary>
      <App />
    </ErrorBoundary>
  </StrictMode>,
);
